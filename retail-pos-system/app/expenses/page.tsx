'use client';
import { useEffect,useState } from 'react';import Shell from '@/components/Shell';import { supabase } from '@/lib/supabase';import { formatNaira,getUser } from '@/lib/auth';
export default function Expenses(){const[items,setItems]=useState<any[]>([]);const[form,setForm]=useState<any>({title:'',amount:0,description:''});useEffect(()=>{if(getUser()?.role!=='admin') location.href='/cashier';load()},[]);async function load(){const{data}=await supabase.from('expenses').select('*').order('created_at',{ascending:false});setItems(data||[])}async function save(e:any){e.preventDefault();await supabase.from('expenses').insert(form);setForm({title:'',amount:0,description:''});load()}return <Shell><div className="card"><h2>Record Expense</h2><form className="form-grid" onSubmit={save}><input className="input" placeholder="Expense title" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/><input className="input" type="number" placeholder="Amount" value={form.amount} onChange={e=>setForm({...form,amount:e.target.value})}/><input className="input" placeholder="Description" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/><button className="btn green">Save Expense</button></form></div><div className="card"><h2>Expenses</h2><table className="table"><thead><tr><th>Date</th><th>Title</th><th>Amount</th><th>Description</th></tr></thead><tbody>{items.map(x=><tr key={x.id}><td>{new Date(x.created_at).toLocaleString()}</td><td>{x.title}</td><td>{formatNaira(x.amount)}</td><td>{x.description}</td></tr>)}</tbody></table></div></Shell>}

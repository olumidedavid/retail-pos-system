'use client';
import { useEffect,useState } from 'react';import Shell from '@/components/Shell';import { supabase } from '@/lib/supabase';import { getUser } from '@/lib/auth';
export default function Users(){const[users,setUsers]=useState<any[]>([]);const[form,setForm]=useState<any>({username:'',password:'',full_name:'',role:'cashier',is_active:true});useEffect(()=>{if(getUser()?.role!=='admin') location.href='/cashier';load()},[]);async function load(){const{data}=await supabase.from('app_users').select('*').order('created_at',{ascending:false});setUsers(data||[])}async function save(e:any){e.preventDefault();await supabase.from('app_users').insert(form);setForm({username:'',password:'',full_name:'',role:'cashier',is_active:true});load()}async function toggle(u:any){await supabase.from('app_users').update({is_active:!u.is_active}).eq('id',u.id);load()}return <Shell><div className="card"><h2>Create Cashier/Admin User</h2><form className="form-grid" onSubmit={save}><input className="input" placeholder="Username" value={form.username} onChange={e=>setForm({...form,username:e.target.value})}/><input className="input" placeholder="Password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/><input className="input" placeholder="Full name" value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})}/><select className="input" value={form.role} onChange={e=>setForm({...form,role:e.target.value})}><option value="cashier">Cashier</option><option value="admin">Admin</option></select><button className="btn green">Create User</button></form><div className="notice">For stronger security later, we can hash passwords. This starter version is made simple for easy shop use.</div></div><div className="card"><h2>Users</h2><table className="table"><thead><tr><th>Name</th><th>Username</th><th>Role</th><th>Status</th><th></th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td>{u.full_name}</td><td>{u.username}</td><td>{u.role}</td><td>{u.is_active?'Active':'Disabled'}</td><td><button className="btn secondary" onClick={()=>toggle(u)}>{u.is_active?'Disable':'Enable'}</button></td></tr>)}</tbody></table></div></Shell>}
